import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'track.me.ang',
  appName: 'track-me-ang',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
