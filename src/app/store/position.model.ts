export interface Position {
    id: number;
    longitude: number;
    latitude: number;
    timestamp: string;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    user_id: number;
}
